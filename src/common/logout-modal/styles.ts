import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    alignItems: 'center',
  },
  confirmationText: {
    fontFamily: 'Poppins-SemiBold',
    color: '#fff',
    fontSize: 16,
    marginTop: 20,
    textAlign: 'center',
  },
  logoutBtn: {
    marginTop: 20,
  },
});
